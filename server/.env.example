# Server-side only — NEVER expose to the browser or commit real values.

# ChatGPT (ChatKit widget)
OPENAI_API_KEY=sk-...
OPENAI_CHATKIT_WORKFLOW_ID=wf_...

# Claude proxy (/api/chat, provider=claude)
ANTHROPIC_API_KEY=sk-ant-...

# Gemini proxy (/api/chat, provider=gemini)
GEMINI_API_KEY=AIza...
GEMINI_MODEL=gemini-2.0-flash

# Session + privacy
SESSION_SECRET=change-me-long-random-string
USER_HASH_SALT=change-me-different-random-string

# Deployment
ALLOWED_ORIGIN=https://eladraz.github.io
PORT=8787
